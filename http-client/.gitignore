# dependencies
/node_modules

# misc
.DS_Store

# env files. The .local variants were listed individually and `.env` alone does not
# cover .env.development / .env.production / .env.test either.
.env*

# secrets and local databases
secrets/
credentials*
*.pem
*.key
*.log
*.sqlite
*.sqlite3
*.db

npm-debug.log*
yarn-debug.log*
yarn-error.log*
